# This file ensures the uploads directory is tracked by git
# The uploads directory is used to store user-uploaded evidence files and audio recordings
# Each user gets their own subdirectory within uploads for security and organization
